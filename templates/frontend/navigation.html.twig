<nav>
    <div class="dashboard-navigation">
    <div class="dashboard-navigation-logo">
            <a href="{{ path('backend_dashboard') }}"><img src="{{ asset('images/logo.png') }}"></a>
        </div>
        <div class="dashboard-navigation-item">
            <a href="{{ path('frontend_dashboard') }}">Dashboard</a>
        </div>
        <div class="dashboard-navigation-item">
            <a href="{{ path('frontend_periods') }}">Periodes</a>
        </div>
        <div class="dashboard-navigation-item">
            <a href="{{ path('frontend_recent') }}">Recent</a>
        </div>
        
        <div class="dashboard-navigation-item">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}

            <a class="navigation-item" href="{{ path('app_logout') }}">Uitloggen <i class="fa fa-sign-out"></i></a>
            {% else %}
            <a class="navigation-item" href="{{ path('app_login') }}">Login <i class="fa fa-sign-in"></i></a>
            {% endif %}
        </div>
    </div>
</nav>